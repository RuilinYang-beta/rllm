Convert the stable model to a JSON object with the following structure: 

* The top-level object has a key "solution".
* "solution" maps to an object with keys "House 1" through "House N", where N is the number of houses given in the puzzle. 
* Each of the "House N" keys maps to an object with exactly X attribute-value pairs, where X is the number of attributes per house given in the puzzle.

Rules: 
* Write attribute values in lowercase.  
* The spaces in multi-word attribute values (e.g. "high school", "science fiction") in the puzzle might be removed or replaced with underscore in ASP program and stable model. Restore them to their original space-delimited form as given in the puzzle.


Reply with the JSON object only, do not include markdown formatting, code fences, or any text apart from the JSON object.